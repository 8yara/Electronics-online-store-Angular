import { CreditpipePipe } from './creditpipe.pipe';

describe('CreditpipePipe', () => {
  it('create an instance', () => {
    const pipe = new CreditpipePipe();
    expect(pipe).toBeTruthy();
  });
});
